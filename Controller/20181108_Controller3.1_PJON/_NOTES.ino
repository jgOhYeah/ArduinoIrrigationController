/*
 * TODO:
 * EEPROM SETTINGS:
 * Make the "All" bays option in the edit eeprom window work
 * Disable the buttons input when waiting for a serial response when setting the eeprom settings.
 * The "% >" in the half pos number entry not displaying properly.
 * Error messages when trying to send the half pos from the slave
 * 
 */
